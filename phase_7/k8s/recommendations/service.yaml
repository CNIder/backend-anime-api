apiVersion: v1
kind: Service
metadata:
  name: recommendations
  namespace: anime-api
spec:
  selector:
    app: recommendations
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: NodePort
