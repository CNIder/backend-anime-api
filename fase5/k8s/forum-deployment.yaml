apiVersion: apps/v1
kind: Deployment
metadata:
  name: forum-service
  namespace: artur-matos
  labels:
    app: forum-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: forum-service
  template:
    metadata:
      labels:
        app: forum-service
    spec:
      containers:
      - name: forum-service
        image: europe-west4-docker.pkg.dev/lucky-starlight-492017-k8/anime-repo/forum-service:latest
        ports:
        - containerPort: 5002
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 5002
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 5002
          initialDelaySeconds: 5
          periodSeconds: 5
        env:
        - name: FLASK_ENV
          value: "production"
---
apiVersion: v1
kind: Service
metadata:
  name: forum-service
  namespace: artur-matos
spec:
  selector:
    app: forum-service
  ports:
  - port: 80
    targetPort: 5002
    protocol: TCP
  type: ClusterIP